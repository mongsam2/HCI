import React from "react"

export const handleKeyEvent = (keyCode: string, setAnimalIdx: React.Dispatch<React.SetStateAction<number>>, setFormIdx: React.Dispatch<React.SetStateAction<number>>) => {
    /*
    패닝키: (패닝키 입력이 들어오면 기본적으로 모습 인덱스는 1로 초기화)
        p1: 동물 인덱스 하나 감소
        p2: 동물 인덱스 하나 증가
    기능키:
        f1: 모습 인덱스 1로 변경
        f2: 모습 인덱스 2로 변경
        f3: 모습 인덱스 3로 변경
        f4: 동물에 대한 간단한 설명을 음성으로 출력
    */
}